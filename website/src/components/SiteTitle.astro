---
import StarlightSiteTitle from "@astrojs/starlight/components/SiteTitle.astro";
import Logo from "./Logo.astro";
---

<div class="docs-title"><Logo size={30} /><StarlightSiteTitle /></div>

<style>
.docs-title {
  --logo-shell: var(--sl-color-bg-nav);
  --logo-border: var(--sl-color-hairline-light);
  --logo-accent: var(--sl-color-accent);
  --logo-paper: var(--sl-color-white);
  display: flex;
  align-items: center;
  gap: 0.65rem;
  min-width: 0;
}
</style>
